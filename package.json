{
  "name": "chatz-ultra",
  "version": "1.0.0",
  "description": "HDTalk - Real-Time Messaging & HD WebRTC Calling System | Created by Himanshu Dwivedi",
  "scripts": {
    "prebuild": "npm --prefix frontend install --include=dev && npm --prefix backend install",
    "build": "npm --prefix frontend run build",
    "start": "node backend/src/server.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm --prefix frontend install --include=dev && npm --prefix backend install",
    "db:reset": "npm --prefix backend run db:reset",
    "db:seed": "npm --prefix backend run db:seed",
    "db:demo-seed": "npm --prefix backend run db:demo-seed",
    "test": "npm --prefix backend run test:unit",
    "claude": "omniroute run claude --model auto/claude-sonnet",
    "omniroute:start": "omniroute serve --daemon",
    "omniroute:status": "omniroute status"
  }
}
